version: "3.7"

services:
  jekyll_site:
    image: jekyll/jekyll:latest
    #command: ["jekyll", "serve", "--watch", "--draft"]
    command: jekyll serve --watch --draft --incremental --config _config.yml,_config_dev.yml --host 0.0.0.0 --verbose
    ports:
      - "4000:4000"
    volumes:
     - type: bind
       source: ../
       target: /srv/jekyll
    # THIS BIT!!!1!
    #user: "1000:1000"

